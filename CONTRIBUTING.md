# 貢献の手引き

このプロジェクトは**規約を文書だけで守らせない**方針を採っています。
守ってほしいことの多くは機械検査になっており、CI で落ちます。

**最初に [`CLAUDE.md`](CLAUDE.md) を読んでください。** 決定事項・禁止事項・作業手順が書かれています。

---

## 1. Issue から始める

**コードより先に Issue を立ててください。** 特に次の場合は必須です。

- 新しい機能や依存を追加する
- 既存の ADR（[`docs/adr/`](docs/adr/)）の決定を変える
- ライセンスに関わる変更
- 対応フォーマットの追加（Phase 3 のスコープ）

`docs/` の決定と矛盾する変更は、**先に ADR を書いて合意する**必要があります。

---

## 2. 絶対禁止

これらは CI で落ちるか、レビューで差し戻されます。

| 禁止 | 理由 |
|---|---|
| **アプリ実行時のネットワーク通信** | 設計上の制約。`QNetworkAccessManager` を include した時点で違反 |
| `src/core` `src/app` にフォーマット名の文字列リテラル（`"png"` 等） | 能力表は実行時に作る。例外は `src/core/FormatId.hpp` の変換関数のみ |
| `src/core` でのファイルアクセス・時刻取得・例外送出・グローバル可変状態 | 純粋関数コアを保つ |
| 依存方向の逆流 | 正は `core → io → app` |
| **警告抑制プラグマ・`NOLINT` の追加** | 警告を残したまま「通った」と報告できないようにする |
| UI のアニメーション / フェード / 自動スクロール | `docs/spec-core.md` §7 |
| `docs/progress/` の既存記述の書き換え | **追記のみ。** 記録は改変しない |
| 品質ゲートを実行せずに「通った」と報告する | — |

---

## 3. 開発の流れ

### 3.1 ブランチ

```bash
git checkout -b <type>/<短い説明>
```

`docs/phases.md` §1 は **1 Phase = 1 ブランチ = 1 PR** と定めています。
Phase 作業でない場合も、変更の単位でブランチを切ってください。

### 3.2 TDD

**テストを先に書きます。** 期待値を先に固定し、それを緩めないでください。

期待値を緩めたくなったら、それは設計か仕様の問題です。**手を止めて Issue で相談してください。**

### 3.3 1 コミット = 1 変更

「ついでに」の修正を混ぜないでください。コミットメッセージは次の形式です。

```
<type>: <説明>

<本文（任意）>
```

`type` は `feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `perf` / `ci`。

### 3.4 品質ゲートを自分で実行する

**PR を出す前に、すべて自分で実行してください。**

```bash
cmake --preset dev && cmake --build --preset dev     # 警告ゼロ（-Werror / MSVC: /WX）
ctest --preset dev --output-on-failure
clang-format --dry-run --Werror $(git ls-files '*.cpp' '*.hpp')
clang-tidy -p build/dev $(git ls-files 'src/*.cpp')
cmake --preset asan && ctest --preset asan --output-on-failure
```

`clang-format` / `clang-tidy` は **CI と同じ版**を使ってください。CI は
`.github/workflows/ci.yml` の `LLVM_VERSION` で固定しています。版が違うと整形結果が食い違います。

> **ローカルの Qt が CI より新しい場合、ローカルで通っても CI で落ちることがあります。**
> CI は Qt 6.8 系 LTS に固定しており、これは 6.8 以降に入った API を無自覚に使う事故を
> 検出するための仕組みです。実例は `docs/progress/phase2.md` の `QStringLiteral` の件。

---

## 4. Pull Request

[`.github/PULL_REQUEST_TEMPLATE.md`](.github/PULL_REQUEST_TEMPLATE.md) の項目を埋めてください。

**特に次の 2 つを空欄にしないでください。**

- **品質ゲートの実行結果** — 実行したコマンドと結果を書く
- **推測で埋めた箇所** — 無ければ「なし」と書く

**確信が持てないまま進めた箇所は、隠さず書いてください。** 推測で進めることが問題であり、
分からないと書くことは問題ではありません。

### 実機確認について

**CI の成功や自動テストの結果を、実機操作の根拠として報告しないでください。**
GUI の実操作（D&D、キーボード操作など）は自動テストで担保できません。
確認していない項目は「未確認」と明記してください。

---

## 5. 迷ったら止まる

次に当てはまったら、**実装せずに Issue で相談してください。**

1. 指示書にない機能・依存が必要だと判断した
2. 文書内に矛盾を見つけた
3. Qt の API の存在・挙動に確信が持てない（公式ドキュメントで確認できない）
4. **テストの期待値を緩めたくなった**
5. 品質ゲートが同じ理由で 2 回連続して落ちた
6. 既存の `docs/progress/` または `docs/adr/` の決定と矛盾する実装をしようとした
7. ライセンスの判断が必要になった
8. 1 つの変更の差分が 400 行を超えそう
9. **「この設計は既存の文書より良い」と判断した**

**9 の改善案は歓迎します。** ただし実装してから提案しないでください。
先に Issue か ADR として書き、合意してから実装します。

**止まることは失敗ではありません。推測で進むことが失敗です。**

---

## 6. ライセンスについて

本体は **GNU General Public License v3.0 or later** です（[`LICENSE`](LICENSE)）。

**貢献されたコードも同じライセンスで配布されます。** PR を出すことで、
あなたの貢献が GPLv3-or-later で配布されることに同意したものとみなします。

GPL と両立しないライセンスのコードを持ち込まないでください。判断に迷う場合は、
**コードを書く前に** Issue で相談してください（[`docs/licenses.md`](docs/licenses.md)）。

---

## 7. 行動規範

[`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md) に従ってください。
