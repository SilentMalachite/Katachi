# セキュリティ

## 報告方法

**脆弱性は公開の Issue に書かないでください。**

GitHub の **Private vulnerability reporting** を使ってください。有効化済みです。

**→ [脆弱性を報告する](https://github.com/SilentMalachite/Katachi/security/advisories/new)**

リポジトリの **Security** タブ → **Report a vulnerability** からも同じ画面に入れます。
GitHub アカウントがあれば誰でも使えます。**やり取りは公開されません。**

修正が用意できたあと、報告者の同意を得てから advisory を公開します。
**公開時に報告者としてクレジットされることを希望するかどうか、報告時に書いてください。**

報告に含めてほしい情報。

- 影響を受けるバージョン（コミットハッシュがあれば確実）
- OS と Qt のバージョン
- 再現手順。**入力ファイルがあると助かりますが、機微な画像は送らないでください**
- 想定される影響

**初回の応答までの目安は 1 週間**です。個人が保守しているため、これより早い保証はできません。

---

## 設計上の性質

報告の前に知っておいてほしい前提です。

### ネットワーク通信を行わない

**このアプリは実行時にネットワーク通信を一切行いません。** これは努力目標ではなく、
プロジェクトの絶対禁止事項です（[`CLAUDE.md`](CLAUDE.md)）。
`QNetworkAccessManager` を include した時点で違反とみなします。

したがって、**通信を前提とした攻撃面は存在しません。**
テレメトリ・自動更新・クラッシュレポート送信もありません。

### 攻撃面は「入力画像」と「出力先パス」

現実的な攻撃面は次の 2 つです。

| 攻撃面 | 内容 |
|---|---|
| **入力画像の復号** | 実際の復号は **Qt の画像プラグイン**（libjpeg-turbo / libtiff / libwebp など）が行います。`KATACHI_EXTRA_CODECS=ON`（既定 OFF、macOS のみ）でビルドした場合は libavif / libjxl / LibRaw / kimageformats も対象に加わります。それらの脆弱性は Qt および各ライブラリの上流の問題です |
| **出力先のパス操作** | 命名パターンと衝突ポリシーの結果として書き込むパス |

**入力画像の復号中のクラッシュは、まず Qt 側の問題である可能性が高いです。**
その場合も報告は歓迎しますが、`docs/progress/` に記録して上流へ回すことがあります。

### 意図的に受け入れているもの

**確保失敗時の `std::terminate`。** コア関数は `noexcept` を維持する設計であり、
メモリ確保に失敗した場合はプロセスが終了します（`docs/adr/0002-noexcept-and-allocation.md`）。

過大な画像による確保失敗を避けるため、**復号より前に `QImageReader::size()` で
画素数の上限を判定**しています。この事前判定を回避できる入力があれば、それは報告に値します。

---

## 対応範囲

| 対象 | 状態 |
|---|---|
| `main` ブランチの最新 | 対応する |
| リリース版 | **未リリース**（配布は Phase 4） |
| Qt 本体・Qt の画像プラグインの脆弱性 | **上流の問題。** Qt のバージョンを上げる対応を行う |

配布物がまだ存在しないため、現時点で報告できるのは**ソースからビルドした場合**の問題です。

---

## 検証に使っているもの

| 手段 | 内容 |
|---|---|
| ASan + UBSan | CI で全テストを実行（macOS） |
| 警告ゼロの強制 | `-Werror` / MSVC は `/WX` |
| clang-tidy | `bugprone-*` / `cppcoreguidelines-*` を含む。**警告抑制コメントの追加を禁止** |

これらを通っていることは、脆弱性が無いことの証明ではありません。
